## Docker Compose

* **launch the stack:** `docker-compose up -d`

* **Verify you can access the app via Nginx from the browser:** `http://localhost:8080`

* **Use docker-compose logs and ps to inspect container behavior.:** `docker compose ps / docker compose logs`

